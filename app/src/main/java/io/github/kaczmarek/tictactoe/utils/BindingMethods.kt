package io.github.kaczmarek.tictactoe.utils

/*
import androidx.databinding.BindingMethod
import androidx.databinding.BindingMethods
import io.github.kaczmarek.tictactoe.utils.components.PlayerTextView

@BindingMethods(
    value = [
        BindingMethod(
            type = PlayerTextView::class,
            attribute = "ptvName",
            method = "setName"
        ),
        BindingMethod(
            type = PlayerTextView::class,
            attribute = "ptvShouldVisibleBorder",
            method = "changeVisibilityBorder"
        )]
)
class BindingMethods*/
